{
    "name": "@story/visualizer-client",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview"
    },
    "comment:dependencies": "Grepped from the moved tree, not guessed. `@emotion/react` is the one entry with no direct import: it is the required peer of `@emotion/styled` (11 direct imports) and of `@mui/material`, so it is declared here rather than relied on through hoisting. The root's `d3`, `jlouvain`, `ngraph.forcelayout`, `ngraph.graph` and `@types/d3` had zero imports anywhere in the repo and were deleted instead of moved (REFACTOR_PLAN §7).",
    "dependencies": {
        "@emotion/react": "^11.13.3",
        "@emotion/styled": "^11.13.0",
        "@mui/icons-material": "^6.1.0",
        "@mui/material": "^6.1.0",
        "@story/core": "workspace:^",
        "@story/data": "workspace:^",
        "@story/shared": "workspace:^",
        "@story/types": "workspace:^",
        "@story/ui": "workspace:^",
        "mobx": "^6.13.2",
        "mobx-react-lite": "^4.0.7",
        "react": "18.3.1",
        "react-dom": "18.3.1"
    },
    "devDependencies": {
        "@types/react": "^18.3.3",
        "@types/react-dom": "^18.3.0",
        "@vitejs/plugin-react": "^4.3.2",
        "vite": "^5.4.9"
    }
}
