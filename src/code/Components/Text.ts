import { styled } from '@mui/material';

export const Text = styled('span')`
    font-size: 16px;
    line-height: 1;
`;

export const Title = styled('span')`
    font-size: 48px;
    line-height: 1;
    font-weight: 500;
`;
